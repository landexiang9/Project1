# C 语言学习笔记

> 本文档基于 `Project1` 项目中的 46 个 C 语言源文件整理而成，
> 覆盖了零基础入门所需的核心知识点，适合初学者系统学习。

---

## 目录

- [笔记 .md 文件结构说明](#笔记-md-文件结构说明)
- [一、C 程序的基本结构](#一c-程序的基本结构)
- [二、注释](#二注释)
- [三、预处理指令](#三预处理指令)
- [四、基本数据类型](#四基本数据类型)
- [五、变量](#五变量)
- [六、常量](#六常量)
- [七、运算符与表达式](#七运算符与表达式)
- [八、输入与输出](#八输入与输出)
- [九、选择结构（分支语句）](#九选择结构分支语句)
- [十、循环结构](#十循环结构)
- [十一、数组](#十一数组)
- [十二、字符串与字符处理](#十二字符串与字符处理)
- [十三、函数](#十三函数)
- [十四、指针（基础概念）](#十四指针基础概念)
- [十五、标准库函数](#十五标准库函数)
- [十六、经典算法](#十六经典算法)
- [附录：ASCII 码对照表（常用部分）](#附录ascii-码对照表常用部分)
- [附录：运算符优先级（从高到低）](#附录运算符优先级从高到低)

---

## 一、C 程序的基本结构

一个 C 程序由一个或多个 **函数** 组成，其中必须且只能有一个名为 `main` 的函数 —— 它是程序的 **入口**。程序从 `main` 的第一条语句开始执行，到 `main` 末尾结束。

### 1.1 `main` 函数的写法

C 标准要求 `main` 返回一个 `int`（整数值），0 表示程序正常结束，非 0 表示异常结束。

```c
int main()
{
    // 代码写在这里
    return 0;
}
```

**项目示例**（`main.c：9-12`）：

```c
int main()
{
    panduandancigeshu();  // 调用其他函数
    return 0;
}
```

> 本项目中的部分练习函数写作 `int 函数名()` 却没有写 `return`，这在旧标准中允许但会产生警告。建议始终为 `int` 返回值函数提供 `return 0;`。

### 1.2 函数调用

在 `main` 中可以调用其他函数。被调用的函数如果定义在后文（或另一个文件中），需要先做 **前向声明**（见第十三章）。

---

## 二、注释

注释用于给代码添加说明，编译时会被忽略。C 语言支持两种注释：

### 2.1 多行注释

```c
/*
 * 这是多行注释
 * 可以跨越多行
 * 常用于文件头部说明
 */
```

**项目示例**（`三个数判断大小并排序.c` 文件头）：

```c
/*
 * 三个浮点数比较与排序
 * 输入三个浮点数 a、b、c，通过两两比较交换实现升序排列。
 */
```

### 2.2 单行注释

```c
int x = 1;  // 这是单行注释，// 之后的内容都是注释
```

**项目示例**（`猴子吃桃.c`）：

```c
int x=1 ,t=0;  // x: 桃子数，初始为第 10 天的 1 个
```

> 单行注释 `//` 是 C99 标准引入的。在 C89 中只支持 `/* */`。本项目使用 `-std=gnu99` 编译。

---

## 三、预处理指令

预处理指令以 `#` 开头，在 **编译之前** 由预处理器处理。

### 3.1 `#include` —— 头文件包含

```c
#include <stdio.h>   // 到系统目录搜索
#include "my.h"      // 先到当前目录搜索
```

导入标准输入输出库 `stdio.h` 后才可以使用 `printf`、`scanf` 等函数。

**项目常用头文件**：

| 头文件 | 用途 | 提供函数 |
|--------|------|---------|
| `stdio.h` | 标准输入输出 | `printf`、`scanf`、`gets`、`puts`、`getchar` |
| `math.h` | 数学函数库 | `pow`、`fabs`、`sqrt`(用 `pow(x,0.5)` 代替) |
| `string.h` | 字符串处理 | `strlen`、`strcmp` |

### 3.2 `#define` —— 宏定义

```
#define 名称 替换内容
```

**项目示例**（`求圆柱表面积和体积.c`）：

```c
#define PEI 3.1415926   // 定义圆周率 π
```

编译前，代码中所有 `PEI` 都会被替换为 `3.1415926`。

### 3.3 `#pragma` —— 编译器指令

```c
#pragma warning(disable:6031)   // 忽略警告 6031（scanf 返回值被忽略）
```

本项目用于屏蔽 Visual Studio 编译器对 `scanf` 返回值的警告。

---

## 四、基本数据类型

### 4.1 整型

| 类型 | 关键字 | 字节（32位） | 范围 | 格式说明符 |
|------|--------|-------------|------|-----------|
| 基本整型 | `int` | 4 | -2³¹ ~ 2³¹-1 | `%d` |
| 短整型 | `short` | 2 | -32768 ~ 32767 | `%hd`（输入 `%hd`） |
| 长整型 | `long` | 4 或 8 | -2³¹~2³¹-1 或更大 | `%ld` |
| 长长整型 | `long long` | 8 | -2⁶³ ~ 2⁶³-1 | `%lld` |

**项目示例**（`整数位数判断.c`）：

```c
long n, m;                     // 声明 long 类型变量
scanf("%ld", &n);              // long 类型用 %ld 读取
printf("整数 %ld 为%d 位整数", m, count);  // %ld 输出
```

### 4.2 浮点型

| 类型 | 关键字 | 字节 | 格式说明符（输入） | 格式说明符（输出） |
|------|--------|------|-------------------|-------------------|
| 单精度 | `float` | 4 | `%f` | `%f` |
| 双精度 | `double` | 8 | `%lf` | `%f` 或 `%lf` |

> **重要**：`scanf` 中 `float` 用 `%f`，`double` **必须** 用 `%lf`。`printf` 中 `%f` 和 `%lf` 效果相同（float 会自动转为 double）。

**项目示例**（`华氏度转摄氏度.c`）：

```c
double f, c;
scanf("%lf", &f);       // double 输入用 %lf
c = (5 * (f - 32)) / 9;
printf("%.2lf", c);     // %.2lf 保留两位小数
```

### 4.3 字符型

| 类型 | 关键字 | 字节 | 格式说明符 |
|------|--------|------|-----------|
| 字符 | `char` | 1 | `%c` |

`char` 本质上存储的是 **ASCII 码**（一个整数，范围 0~127），因此 `char` 可以参与整数运算。

**项目示例**（`将单个字符转换为大写.c`）：

```c
char a;
scanf(" %c", &a);       // 读取一个字符（前面加空格会跳过空白符）
if (t >= 97)            // 97 是 'a' 的 ASCII 码
    printf("%c", t - 32);  // 大写字母 ASCII 码比小写小 32
```

### 4.4 `void` 类型

表示"无类型"，用于：
- 函数没有返回值：`void maopaoapixu() { ... }`
- 函数没有参数：`int func(void) { ... }`

---

## 五、变量

### 5.1 声明与初始化

```c
int a;              // 声明变量 a，此时值不确定（垃圾值）
int b = 10;         // 声明并初始化为 10
int c = 0, d = 5;   // 同时声明多个变量
```

**项目示例**（`判断单词个数.c`）：

```c
char str[100];
int i, count = 0;   // i 未初始化（稍后被 for 赋值），count 初始化为 0
```

### 5.2 变量命名规则

- 由字母（a-z, A-Z）、数字（0-9）、下划线（_）组成
- **不能以数字开头**
- 区分大小写（`count` 和 `Count` 是不同的变量）
- 不能使用 C 语言关键字（如 `int`、`if`、`for`）

### 5.3 变量的作用域

- **局部变量**：在函数内部声明，只能在函数内使用
- **全局变量**：在所有函数外部声明，整个文件都可以使用（本项目未使用全局变量）
- **静态局部变量**：加 `static` 关键字，函数调用结束后值不被销毁（未在项目中使用）

### 5.4 `static` 关键字

函数前加 `static` 表示该函数只在 **当前文件内** 可见，其他文件无法调用。

**项目示例**（`累加.c`）：

```c
static int leijia()   // 此函数仅在本文件中可用
{
    // ...
}
```

---

## 六、常量

### 6.1 宏常量

使用 `#define` 定义：

```c
#define PEI 3.1415926   // 编译时直接替换
```

### 6.2 字符常量

用单引号括起来：`'A'`、`'0'`、`'\n'`

示例：

```c
if (ch >= 'A' && ch <= 'Z')   // 判断是否为大写字母
```

> `'A'` 的值就是其 ASCII 码 65，可以直接参与运算。

### 6.3 字符串常量

用双引号括起来：`"hello"`

字符串常量末尾自动添加 `'\0'`（空字符），所以 `"hello"` 实际占 6 个字节。

### 6.4 转义字符

以 `\` 开头，表示特殊含义：

| 转义序列 | 含义 | ASCII 码（十进制） |
|---------|------|-------------------|
| `\n` | 换行 | 10 |
| `\t` | 制表符 | 9 |
| `\"` | 双引号 | 34 |
| `\\` | 反斜杠 | 92 |
| `\0` | 空字符 | 0 |
| `\40` | 八进制空格 | 32 |
| `\x2a` | 十六进制 `*` | 42 |

**项目示例**（`长方形周长计算.c`）：

```c
b = '\43';       // 八进制 43 = 十进制 35 → '#'
c = '\103';      // 八进制 103 = 十进制 67 → 'C'
d = '\x2a';      // 十六进制 2a = 十进制 42 → '*'
e = '\x4c';      // 十六进制 4c = 十进制 76 → 'L'
printf("\"good night\40 \n");  // 输出: "good night" 然后换行
```

---

## 七、运算符与表达式

### 7.1 算术运算符

| 运算符 | 含义 | 示例 | 结果 |
|--------|------|------|------|
| `+` | 加法 | `3 + 2` | `5` |
| `-` | 减法 | `3 - 2` | `1` |
| `*` | 乘法 | `3 * 2` | `6` |
| `/` | 除法 | `3 / 2` | `1`（**整数除法**截断小数） |
| `/` | 除法（浮点） | `3.0 / 2` | `1.5` |
| `%` | 取模（取余数） | `5 % 2` | `1` |

> **整数除法陷阱**：`1/2` 结果是 0 而不是 0.5！至少一个操作数为浮点数才会做浮点除法。

**项目示例**（`n分之1累加.c`）：

```c
count = count + 1.0 / i;   // 1.0 / i 是浮点除法
// 如果写 1 / i，当 i > 1 时结果永远是 0！
```

### 7.2 关系运算符

| 运算符 | 含义 | 示例 |
|--------|------|------|
| `>` | 大于 | `a > b` |
| `<` | 小于 | `a < b` |
| `>=` | 大于等于 | `a >= b` |
| `<=` | 小于等于 | `a <= b` |
| `==` | 等于 | `a == b` |
| `!=` | 不等于 | `a != b` |

关系表达式的值是整数：条件成立为 **1**（真），不成立为 **0**（假）。

### 7.3 逻辑运算符

| 运算符 | 含义 | 示例 | 说明 |
|--------|------|------|------|
| `&&` | 逻辑与（且） | `a > 0 && a < 100` | 两个都为真才为真 |
| `\|\|` | 逻辑或（或） | `a == 0 \|\| b == 0` | 一个为真就为真 |
| `!` | 逻辑非（取反） | `!a` | 真变假，假变真 |

**短路求值**（重要）：对于 `a && b`，如果 `a` 为假，`b` **不会被执行**；对于 `a || b`，如果 `a` 为真，`b` **不会被执行**。

**项目示例**（`源6.c`）：

```c
int a = 3, b = 3, c = 0;
printf("%d", !c);                 // !0 = 1，输出 1
printf("%d", !a && !b && c);      // !a=0，&& 短路，结果为 0
d = c-- || (b == 6) && a++;
// 实际运算顺序：&& 优先级高于 ||，先算 (b == 6) && a++（值为 0）
// 然后 c-- || 0，c-- 取值为 0（假），继续算 0 || 0 = 0
// a++ 没有执行（因为 && 左侧 b==6 为假，短路），所以 a 保持 3
// c-- 先取值 c=0 再自减，所以 c = -1
```

**重点理解**：在项目代码 `源6.c` 中：

```c
d = c-- || (b == 6) && a++;
// 解析：&& 优先级高于 ||
// 1. c-- 是后缀自减，先取 c 的当前值 0 参与运算，然后 c 变为 -1
// 2. c-- 的值是 0（假），所以 || 需要继续看右边 (b==6) && a++
// 3. b==6 是假（b=3），导致 && 短路，a++ 不执行，a 保持 3
// 4. (b==6) && a++ 的结果是 0
// 5. 0 || 0 = 0，所以 d = 0
// 结果：a=3, b=3, c=-1, d=0
```

### 7.4 自增自减运算符

| 运算符 | 名称 | 示例 | 相当于 | 返回值 |
|--------|------|------|--------|--------|
| `++i` | 前缀自增 | `t = ++i` | `i = i+1; t = i` | 自增后的值 |
| `i++` | 后缀自增 | `t = i++` | `t = i; i = i+1` | 自增前的值 |
| `--i` | 前缀自减 | `t = --i` | `i = i-1; t = i` | 自减后的值 |
| `i--` | 后缀自减 | `t = i--` | `t = i; i = i-1` | 自减前的值 |

**项目示例**（`源3.c`）：

```c
int s = 65, n = 1, t;
t = ++s + n++;          // ++s: s 先变成 66，n++: 取 n=1 再加
printf("s=%c s=%d n=%d t=%c\n", s, s, n, t);
// s=66='B', n=2, t=66+1=67='C'

t = ++s * --n % s;     // 未定义行为！同一个表达式中 s 被多次修改
```

> **警告**：在同一表达式中多次修改同一个变量会导致 **未定义行为**（结果因编译器而异）。如 `t = ++s * --n % s`。应避免这样写。

### 7.5 条件运算符（三目运算符）

```
条件 ? 表达式1 : 表达式2
```

如果条件为真（非 0），整个表达式的值是 `表达式1`，否则是 `表达式2`。

**项目示例**（`将单个字符转换为大写.c`）：

```c
// 用三目运算符实现小写转大写
printf("转换后的字符为%c", s >= 97 ? s - 32 : s);

// 等价于：
if (s >= 97)
    printf("%c", s - 32);
else
    printf("%c", s);
```

### 7.6 赋值运算符

| 运算符 | 示例 | 等价于 |
|--------|------|--------|
| `=` | `a = 5` | 直接赋值 |
| `+=` | `a += 3` | `a = a + 3` |
| `-=` | `a -= 3` | `a = a - 3` |
| `*=` | `a *= 3` | `a = a * 3` |
| `/=` | `a /= 3` | `a = a / 3` |
| `%=` | `a %= 3` | `a = a % 3` |

### 7.7 `sizeof` 运算符

`sizeof` 是一个运算符（不是函数），返回变量或类型占用的 **字节数**，返回值类型为 `size_t`。

```c
printf("%zu", sizeof(int));     // 输出 4
printf("%zu", sizeof(double));  // 输出 8
```

**项目示例**（`源4.c`）：

```c
int a = 3;
float c;
printf("%zu,%zu,%zu", sizeof(a), sizeof(b), sizeof(c));
// 可能输出: 4,4,4 （在 32 位系统上 int 4 字节，float 4 字节）
```

> `%zu` 是专门用于格式化输出 `size_t` 类型的说明符。

### 7.8 类型转换

#### 隐式类型转换

当不同类型参与运算时，C 会自动将"低精度"类型转为"高精度"类型：

```
int → long → long long → float → double → long double
```

#### 强制类型转换

```c
(类型名)表达式
```

**项目示例**（`源4.c`）：

```c
int a = 3, b = 4, e = 2;
float c, d, g;

c = (float)(a + b) / 2;     // a+b=7 → 转 float → 7.0/2=3.5 （正确）
d = (float)(a / e);          // a/e=1（整数除法截断）→ 转 float → 1.0 （错误！）
g = (float)(a / (float)e);   // e 先转 float → 3.0/2.0=1.5 （正确）
```

**关键区别**：
- `(float)(a / e)` —— 先做整数除法（结果为 1），再转为 float（1.0）
- `(float)a / e` ———— 先将 a 转为 float（3.0），再做浮点除法（1.5）

### 7.9 运算符优先级（关键部分）

| 优先级 | 运算符 | 结合性 |
|--------|--------|--------|
| 高（1） | `()` `[]` | 从左到右 |
| 2 | `++` `--`（后缀） | 从左到右 |
| 3 | `++` `--`（前缀） `!` `(类型)` `sizeof` | 从右到左 |
| 4 | `*` `/` `%` | 从左到右 |
| 5 | `+` `-` | 从左到右 |
| 6 | `<` `<=` `>` `>=` | 从左到右 |
| 7 | `==` `!=` | 从左到右 |
| 8 | `&&` | 从左到右 |
| 9 | `\|\|` | 从左到右 |
| 10 | `?:` | 从右到左 |
| 低（11） | `=` `+=` `-=` 等 | 从右到左 |

> **牢记**：`&&` 优先级高于 `||`。有疑问时加括号明确。

---

## 八、输入与输出

### 8.1 `printf` —— 格式化输出

```c
printf("格式字符串", 参数1, 参数2, ...);
```

#### 常用的格式说明符

| 说明符 | 对应类型 | 示例 |
|--------|---------|------|
| `%d` | `int` | `printf("%d", 10);` |
| `%hd` | `short` | `printf("%hd", s);` |
| `%ld` | `long` | `printf("%ld", l);` |
| `%lld` | `long long` | `printf("%lld", ll);` |
| `%f` | `float` / `double` | `printf("%f", 3.14);` |
| `%lf` | `double` | `printf("%lf", 3.14);` |
| `%c` | `char` | `printf("%c", 'A');` |
| `%s` | 字符串 | `printf("%s", str);` |
| `%zu` | `size_t`（`sizeof` 结果） | `printf("%zu", sizeof(int));` |

#### 格式修饰符

```c
%5d         // 宽度至少 5，右对齐
%5.2f       // 总宽度 5，小数保留 2 位
%-5d        // 左对齐
%05d        // 前导补零
```

**项目示例**（`三个数判断大小并排序.c`）：

```c
printf("%5.2f,%5.2f,%5.2f\n", a, b, c);  // 每个数占 5 格，保留 2 位小数
```

**项目示例**（`输入二十个成绩并排序.c`）：

```c
printf("\n平均分为%.2f", avg);   // 保留两位小数
```

### 8.2 `scanf` —— 格式化输入

```c
scanf("格式字符串", &变量1, &变量2, ...);
```

> **重要**：必须加 `&` 取地址！变量前不加 `&` 会导致程序崩溃。

**项目示例**（`判断是否为偶数.c`）：

```c
int a;
scanf("%d", &a);   // 注意 &a
```

#### `scanf` 格式说明符的特殊修饰符

| 修饰符 | 含义 | 示例 |
|--------|------|------|
| `%hd` | 读取 `short` | `scanf("%hd", &b);` |
| `%ld` | 读取 `long` | `scanf("%ld", &n);` |
| `%lf` | 读取 `double` | `scanf("%lf", &x);` |

#### 输入宽度限定

**项目示例**（`源5.c`）：

```c
scanf("a,b:%4d%2hd", &a, &b);  // 最多读 4 位给 a，最多读 2 位给 b
scanf("%5f,%*d,%6lf", &c, &x); // 读 5 位给 c，跳过 2 位整数，读 6 位给 x
```

- `%4d`：最多读取 4 位数字给 `int`
- `%2hd`：最多读取 2 位数字给 `short`
- `%*d`：读取一个整数并 **丢弃**（不存储）
- `"a,b:"` 在格式字符串中表示 **匹配字面字符**

#### `scanf` 的常见问题

```c
char ch;
scanf("%c", &ch);   // 会读取到上一次输入残留的换行符！
scanf(" %c", &ch);  // 格式字符串前加空格，可以跳过空白符
```

**项目示例**（`将单个字符转换为大写.c`）：

```c
scanf(" %c", &a);   // 前面的空格会跳过换行符等空白字符
```

### 8.3 `getchar` —— 逐个字符读取

```c
int ch = getchar();   // 从标准输入读取一个字符
```

**项目示例**（`取十个字符判断各种字符的个数.c`）：

```c
for (i = 0; i < 10; i++)
{
    ch = getchar();   // 每次读取一个字符
    // 判断并分类统计
}
```

### 8.4 `puts` —— 输出字符串并换行

```c
puts("hello");   // 等价于 printf("hello\n");
puts(c1);        // 输出字符数组中的字符串
```

### 8.5 `gets` —— 读取一行字符串（警告：不安全）

```c
gets(str);   // 读取一行到字符数组 str 中，不检查长度
```

> `gets` 不检查缓冲区长度，容易导致 **缓冲区溢出**，在 C11 标准中已被移除。本项目中使用它是因为它是初学者最直观的字符串输入方式。实际开发中应使用 `fgets`。

---

## 九、选择结构（分支语句）

### 9.1 `if` / `else if` / `else`

```c
if (条件) {
    // 条件为真时执行
} else if (条件) {
    // 第一个条件为假，此条件为真时执行
} else {
    // 所有条件都为假时执行
}
```

- **条件**：任何整数表达式，0 为假，非 0 为真
- 花括号可以省略（但不推荐）：当只有一条语句时

**项目示例**（`分数判断.c`）：

```c
if (a > 90)
    printf("wow!,天才你竟然有%d的高分", a);
else if (a <= 90 && a >= 80)
    printf("不戳不戳有%d分", a);
else if (a >= 70)
    printf("还行");
else if (a >= 60)
    printf("努努力，你也能成为人中龙凤");
else if (a >= 0)
    printf("革命尚未成功，同志仍需努力");
else
    printf("你是天才还是人才");
```

> 注意：`else if` 并不是 C 语言的关键字，它实际上就是 `else { if(...) {...} }` 的简写。

### 9.2 `switch` / `case` / `break`

```c
switch (整型表达式) {
    case 常量1:
        // 代码
        break;
    case 常量2:
        // 代码
        break;
    default:
        // 所有 case 都不匹配时执行
}
```

**关键概念**：

| 关键字 | 作用 |
|--------|------|
| `switch` | 对整型表达式进行多路分支 |
| `case` | 定义分支入口，case 后必须跟 **常量** |
| `break` | 跳出 `switch` 块（不写会继续执行下一个 case —— 称"**穿透/fall-through**"） |
| `default` | 可选，当没有 case 匹配时执行 |

**项目示例**（`判断月份天数.c`）：

```c
switch (m) {
    case 1:     // 1 月没有 break，会"穿透"到 3 月？不，是 1/3/5/7/8/10/12 都走 d=31
    case 3:
    case 5:
    case 7:
    case 8:
    case 10:
    case 12:
        d = 31; break;   // 遇到 break 才跳出
    case 4:
    case 6:
    case 9:
    case 11:
        d = 30; break;
    case 2:
        // 闰年判断
        if (y % 400 == 0 || (y % 4 == 0 && y % 100 != 0))
            d = 29;
        else
            d = 28;
        break;
    default:
        d = 0; break;     // 月份输入错误
}
```

> 利用 case 的 **穿透（fall-through）** 特性，多个 case 共享同一段代码是很常见的用法。

---

## 十、循环结构

### 10.1 `for` 循环

```c
for (初始化; 条件; 更新) {
    // 循环体
}
```

执行顺序：
1. **初始化**（仅在第一次执行）
2. **判断条件**，如果为假则退出循环
3. **执行循环体**
4. **更新**
5. 回到第 2 步

**项目示例**（`三个数判断大小并排序.c` 的变体思想 + `九九乘法表.c`）：

```c
// 典型的 for 循环
for (i = 0; i < 10; i++) {
    printf("%d ", i);
}

// 九九乘法表中的嵌套 for 循环
for (i = 1; i <= 9; i++) {
    for (j = 1; j <= i; j++) {
        printf("%d*%d=%d ", j, i, i * j);
    }
    printf("\n");
}
```

**for 循环的省略写法**：

```c
int i = 0;
for (; i < 10; ) {    // 省略初始化和更新
    // ...
    i++;
}

for (;;) {            // 无限循环（相当于 while(1)）
    // ...
}
```

### 10.2 `while` 循环

```c
while (条件) {
    // 循环体
}
```

先判断条件，条件为真才执行循环体。如果一开始条件就为假，循环体一次都不执行。

**项目示例**（`累乘while.c`）：

```c
while (i <= n) {
    fact = fact * i;
    i = i + 1;
}
```

### 10.3 `do-while` 循环

```c
do {
    // 循环体
} while (条件);
```

**先执行一次循环体，再判断条件**。至少执行一次。

**项目示例**（`累乘do-wile.c`）：

```c
do {
    fact = fact * i;
    i = i + 1;
} while (i <= n);
```

### 10.4 三种循环的对比

| 特点 | `for` | `while` | `do-while` |
|------|-------|---------|-----------|
| 初始化 | 在语句中 | 在之前 | 在之前 |
| 条件判断时机 | 进入前 | 进入前 | 执行后 |
| 至少执行一次 | 否 | 否 | **是** |
| 适用场景 | 确定次数 | 不确定次数 | 至少需要执行一次 |

### 10.5 `break` —— 跳出循环

```c
for (i = 0; i < 10; i++) {
    if (i == 5) break;   // i 等于 5 时立即结束循环
}
// 此时 i = 5
```

### 10.6 `continue` —— 跳过本次循环剩余部分

```c
for (n = 1; n <= 100; n++) {
    if (n % 7 != 0) continue;  // 不能被 7 整除就跳过后续代码
    printf("%d ", n);           // 只有能被 7 整除的数才会被执行
}
```

**项目示例**（`找出一百以内能被7整除的数.c`）：

```c
if (s == 5) {
    printf("%d\n", n);
    s = 0;
    continue;       // 跳过后面的空格输出，直接进入下一轮
}
printf("%d ", n);
```

### 10.7 `goto` —— 无条件跳转

```c
goto label;
// 被跳过的代码
label:
// 跳转到这里
```

**项目示例**（`找出一百以内能被7整除的数.c`）：

```c
if (s == 5) {
    printf("%d\n", n);
    s = 0;
    goto a;         // 跳转到 a 标签
}
printf("%d ", n);
a: ;                // 标签，空语句
```

> `goto` 会使程序结构混乱，绝大多数情况下可以用 `break`、`continue` 或函数代替。**不推荐在复杂程序中使用**。

---

## 十一、数组

### 11.1 一维数组

```c
类型 数组名[元素个数];
```

**项目示例**（`冒泡排序.c`）：

```c
int a[10];           // 声明一个包含 10 个 int 的数组
a[0] = 5;            // 第一个元素（索引从 0 开始）
a[9] = 3;            // 最后一个元素
```

#### 数组的初始化

```c
int a[5] = {1, 2, 3, 4, 5};
int a[]  = {1, 2, 3};       // 自动推断大小
int a[5] = {1, 2};          // 前 2 个初始化为 1,2，后 3 个为 0
```

#### 数组的遍历

```c
for (i = 0; i < 10; i++) {
    scanf("%d", &a[i]);      // 读取到数组
}

for (i = 0; i < 10; i++) {
    printf("%d ", a[i]);     // 输出数组
}
```

#### 数组作为函数参数

**项目示例**（`交换排序调用.c`）：

```c
int jiaohuanpaixudiaoyong(int n, int a[])  // a[] 接收数组
{
    // 可以直接修改 a[0], a[1] 等，修改会反映到原数组
}

// 调用：
jiaohuanpaixudiaoyong(10, a);  // 数组名作为参数
```

> 注意：数组作为参数传递时，实际上传递的是 **首地址**（指针），函数内修改数组元素会影响原数组。同时需要**额外传递数组长度**，因为函数内部无法知道数组大小。

### 11.2 二维数组

```c
类型 数组名[行数][列数];
```

**项目示例**（`杨辉三角.c`）：

```c
int a[6][6];             // 6 行 6 列的二维数组
a[t][0] = 1;             // 第 t 行第 0 列的元素
a[t][t] = 1;             // 第 t 行第 t 列的元素
a[t][i] = a[t-1][i-1] + a[t-1][i];  // 使用上一行相邻两元素
```

遍历二维数组：

```c
for (t = 0; t < 6; t++) {
    for (i = 0; i <= t; i++) {
        printf("%d ", a[t][i]);
    }
    printf("\n");
}
```

---

## 十二、字符串与字符处理

### 12.1 字符数组与字符串

C 语言中**没有专门的字符串类型**，字符串用字符数组来存储，以 `'\0'`（空字符，ASCII 码 0）结尾。

```c
char str[100];           // 可以存储最多 99 个字符 + 1 个 '\0'
char str[] = "hello";    // 自动分配 6 字节：'h','e','l','l','o','\0'
char str[] = {'h','e','l','l','o','\0'};  // 与上面等价
```

### 12.2 字符串输入

**`gets`（简单但不安全）**：

```c
gets(str);   // 读取一行到 str，直到遇到换行符
```

**`scanf` 的 `%s`（以空格分隔）**：

```c
scanf("%s", str);   // 读取到遇空格或换行，注意 str 前不加 &
```

### 12.3 字符串处理函数（来自 `<string.h>`）

| 函数 | 功能 | 示例 |
|------|------|------|
| `strlen(s)` | 返回字符串长度（不含 `\0`） | `n = strlen("abc");` // 3 |
| `strcmp(s1, s2)` | 比较两个字符串 | `if (strcmp(s1, s2) == 0)` // 相等 |
| `strcpy(dest, src)` | 复制字符串 | `strcpy(c2, c1);` |
| `strcat(dest, src)` | 拼接字符串 | `strcat(s1, s2);` |

#### `strcmp` 的返回值

| 返回值 | 含义 |
|--------|------|
| `< 0` | s1 小于 s2（字典序） |
| `== 0` | s1 等于 s2 |
| `> 0` | s1 大于 s2（字典序） |

**项目示例**（`回文串判断.c`）：

```c
if (strcmp(c1, c2) == 0)    // 判断两个字符串是否完全相同
    printf("same");
else
    printf("not same");
```

### 12.4 字符串遍历

**项目示例**（`判断单词个数.c`）：

```c
for (i = 0; str[i] != '\0'; i++)   // 遍历到结束符
{
    if (str[i] == ' ' && str[i + 1] != ' ' && str[i + 1] != '\0')
    {
        count++;  // 遇到空格且下一个不是空格也不是结束符 → 单词分隔
    }
}
```

### 12.5 字符串的结束符 `'\0'`

```c
c2[t] = '\0';   // 手动添加字符串结束符，否则 strcmp 会继续读取后面的垃圾值
```

> 对字符数组操作时，**必须确保末尾有 `'\0'`**，否则字符串函数会越界访问。

### 12.6 ASCII 码与字符判断

字符在内存中以 ASCII 码（整数）形式存储：

| 类别 | ASCII 码范围 | 判断写法 |
|------|-------------|---------|
| 大写字母 | 65~90（'A'~'Z'） | `ch >= 'A' && ch <= 'Z'` |
| 小写字母 | 97~122（'a'~'z'） | `ch >= 'a' && ch <= 'z'` |
| 数字 | 48~57（'0'~'9'） | `ch >= '0' && ch <= '9'` |

大小写转换：**大写 = 小写 − 32**（因为 `'a' - 'A' = 32`）

**项目示例**（`取十个字符判断各种字符的个数.c`）：

```c
if (ch >= 'A' && ch <= 'Z')
    big = big + 1;           // 大写字母
else if (ch >= 'a' && ch <= 'z')
    small = small + 1;       // 小写字母
else if (ch >= '0' && ch <= '9')
    num = num + 1;           // 数字
else
    oth = oth + 1;           // 其他字符
```

### 12.7 `puts` 输出字符串

```c
puts(c1);   // 输出 c1 的内容并自动换行
```

---

## 十三、函数

### 13.1 函数的定义

```c
返回值类型 函数名(参数列表) {
    // 函数体
    return 返回值;
}
```

**项目示例**（`计算圆的面积.c`）：

```c
double yuandemianji(double r)   // 返回 double，接受一个 double 参数
{
    double s;
    s = PEI * r * r;
    return s;                   // 返回面积值
}
```

### 13.2 函数的声明（前向声明）

如果函数的定义在使用之后，或者定义在另一个文件中，需要**提前声明**函数。

```c
返回值类型 函数名(参数类型列表);
```

**项目示例**（`main.c`）：

```c
void panduandancigeshu();       // 声明该函数在别处定义

int main()
{
    panduandancigeshu();        // 虽然定义在另一个文件，但已有声明
    return 0;
}
```

**项目示例**（`计算x的n次方除以n的阶乘之和.c`）：

```c
double coutcheng(int n);        // 声明定义在累乘while.c 中的函数

int jishuangnchujiecheng()
{
    // ...
    fenmu = fabs(coutcheng(t)); // 调用另一个文件的函数
    // ...
}
```

### 13.3 函数参数

**值传递**：C 语言中函数参数默认是"值传递"——函数内修改参数不会影响原变量。

**传递数组时**：传递的是数组首地址，所以函数内可以修改原数组元素。

```c
void func(int n, int a[])   // n 是传值，a[] 传地址
{
    n = 100;                // 不影响调用者的 n
    a[0] = 999;             // 会影响调用者的数组！
}
```

### 13.4 `return` 语句

- `return 值;` — 返回一个值并退出函数
- `return;` — 仅退出函数（用于 `void` 函数）

### 13.5 `void` 函数

不返回值的函数，声明为 `void`：

```c
void maopaoapixu()
{
    // 没有 return
}
```

### 13.6 `static` 函数

```c
static int leijia()      // 只能在本文件中调用
{
    // ...
}
```

---

## 十四、指针（基础概念）

指针是 C 语言的核心概念，本项目只涉及了指针的最基础用法。

### 14.1 取地址运算符 `&`

```c
int a;
scanf("%d", &a);   // &a 表示变量 a 的地址
```

`scanf` 必须传入变量的地址，才能将输入值写入变量。

---

## 十五、标准库函数

### 15.1 `<math.h>` 数学函数

| 函数 | 作用 | 示例 |
|------|------|------|
| `pow(x, y)` | 计算 x 的 y 次幂（返回 `double`） | `pow(2, 3)` → 8.0 |
| `fabs(x)` | 计算 double 的绝对值 | `fabs(-3.5)` → 3.5 |
| `pow(x, 0.5)` | 求平方根（无专门的 `sqrt` 时） | `pow(9, 0.5)` → 3.0 |

**项目示例**（`求sinx的近似值.c`）：

```c
fz = pow(x, n);              // x 的 n 次方
while (fabs(fz / fm) > 1e-6);// 末项绝对值小于 1e-6 时停止
```

> 使用 `math.h` 编译时需要链接数学库：`gcc -lm`。

### 15.2 输入输出函数

| 函数 | 头文件 | 作用 |
|------|--------|------|
| `printf` | `stdio.h` | 格式化输出 |
| `scanf` | `stdio.h` | 格式化输入 |
| `getchar` | `stdio.h` | 读取一个字符 |
| `puts` | `stdio.h` | 输出字符串加换行 |
| `gets` | `stdio.h` | 读取一行字符串（已被弃用） |

### 15.3 字符串函数

| 函数 | 头文件 | 作用 |
|------|--------|------|
| `strlen(s)` | `string.h` | 返回字符串长度 |
| `strcmp(s1, s2)` | `string.h` | 比较两个字符串 |

---

## 十六、经典算法

### 16.1 排序算法

#### 冒泡排序（升序）

核心思想：相邻元素两两比较，较大的"冒泡"到数组末尾。每轮确定一个最大元素的位置。

```c
for (i = 0; i < n - 1; i++) {
    for (j = 0; j < n - 1 - i; j++) {
        if (a[j] > a[j + 1]) {
            t = a[j]; a[j] = a[j + 1]; a[j + 1] = t;
        }
    }
}
```

**项目文件**：`冒泡排序.c`、`输入10个整数排序.c`、`输入二十个成绩并排序.c`

#### 交换排序（降序）

核心思想：固定当前位置 i，与后面每个元素比较，若更大则交换。

```c
for (i = 0; i < n - 1; i++) {
    for (j = i + 1; j < n; j++) {
        if (a[i] < a[j]) {     // 降序
            t = a[i]; a[i] = a[j]; a[j] = t;
        }
    }
}
```

**项目文件**：`交换排序1.c`、`交换排序调用.c`

#### 三个数比较排序

两两比较并交换，确保 a ≤ b ≤ c：

```c
if (a > b) { t = a; a = b; b = t; }
if (a > c) { t = a; a = c; c = t; }
if (b > c) { t = b; b = c; c = t; }
```

**项目文件**：`三个数判断大小并排序.c`

### 16.2 查找算法

#### 找最大值（擂台法）

```c
scanf("%f", &a);
max = a;                 // 第一个数当作"擂主"
for (i = 2; i <= 10; i++) {
    scanf("%f", &b);
    if (b > max)         // 挑战成功，更新擂主
        max = b;
}
```

**项目文件**：`输入多个数字找最大数.c`

### 16.3 递推与迭代

#### 斐波那契数列（兔子繁殖）

```c
int a = 0, b = 1, c;
for (m = 1; m <= n; m++) {
    c = a + b;      // 当月 = 前两月之和
    a = b;          // 滚动更新
    b = c;
}
```

**项目文件**：`兔子繁殖问题.c`

#### 猴子吃桃（逆推）

```c
int x = 1;          // 第 10 天剩余 1 个
for (t = 0; t < 9; t++)
    x = (x + 1) * 2;  // 逆推公式
```

**项目文件**：`猴子吃桃.c`

### 16.4 数学问题

#### 水仙花数（阿姆斯特朗数）

```c
b = a / 100;           // 百位
c = (a - b * 100) / 10; // 十位
a = a - b * 100 - c * 10; // 个位（重用了 a）
if (b*b*b + c*c*c + a*a*a == d)  // 立方和等于原数
    printf("是水仙花数");
```

**项目文件**：`水仙花数判断.c`

#### 一元二次方程求根

```c
double delta = b*b - 4*a*c;
if (delta >= 0) {
    x1 = (-b + pow(delta, 0.5)) / (2*a);
    x2 = (-b - pow(delta, 0.5)) / (2*a);
}
```

**项目文件**：`解二次方程.c`

#### 闰年判断

```c
if ((y % 400 == 0) || (y % 4 == 0 && y % 100 != 0))
    // 闰年
else
    // 平年
```

**项目文件**：`闰年判断.c`、`判断月份天数.c`

#### 阶乘

```c
double fact = 1;
for (i = 1; i <= n; i++)
    fact = fact * i;
```

**项目文件**：`累乘do-wile.c`、`累乘while.c`

#### 泰勒级数求 sin(x)

```c
do {
    fz = pow(x, n);        // x^n
    fm = jiecheng(n);      // n!
    n += 2;
    sum += s * (fz / fm);
    s *= -1;               // 交替符号
} while (fabs(fz / fm) > 1e-6);
```

**项目文件**：`求sinx的近似值.c`

#### 泰勒级数求 e^x

```c
do {
    fenzi = pow(x, t);
    fenmu = coutcheng(t);
    shang = fenzi / fenmu;
    count += shang;
    t++;
} while (fabs(shang) > 1e-4);
```

**项目文件**：`计算x的n次方除以n的阶乘之和.c`

### 16.5 图形输出

#### 杨辉三角

```c
for (t = 0; t < 6; t++) {
    a[t][0] = a[t][t] = 1;              // 首尾为 1
    for (i = 1; i < t; i++)
        a[t][i] = a[t-1][i-1] + a[t-1][i];  // 递推
}
```

**项目文件**：`杨辉三角.c`

#### 金字塔

```c
for (i = 1; i <= n; i++) {
    for (j = 1; j <= n - i; j++)  printf(" ");      // 空格
    for (j = 1; j <= 2 * i - 1; j++)  printf("*"); // 星号
    printf("\n");
}
```

**项目文件**：`金字塔.c`

#### 九九乘法表

```c
for (i = 1; i <= 9; i++) {
    for (j = 1; j <= i; j++) {
        printf("%d*%d=%d ", j, i, i*j);
    }
    printf("\n");
}
```

**项目文件**：`九九乘法表.c`

### 16.6 回文串判断

```c
n = strlen(c1) - 1;
for (t = 0; n >= 0; n--, t++)
    c2[n] = c1[t];
c2[t] = '\0';
if (strcmp(c1, c2) == 0)
    printf("是回文串");
```

**项目文件**：`回文串判断.c`

### 16.7 单词计数

```c
for (i = 0; str[i] != '\0'; i++) {
    if (str[i] == ' ' && str[i+1] != ' ' && str[i+1] != '\0')
        count++;
}
if (i > 0 && str[0] != ' ')
    count++;   // 补上第一个单词
```

**项目文件**：`判断单词个数.c`

### 16.8 整数位数判断

```c
do {
    n = n / 10;      // 去掉最右边一位
    count++;         // 位数加 1
} while (n != 0);
```

**项目文件**：`整数位数判断.c`

---

## 附录：ASCII 码对照表（常用部分）

| 十进制 | 字符 | 说明 |
|-------|------|------|
| 0 | `\0` | 空字符（字符串结束标志） |
| 10 | `\n` | 换行 |
| 32 | （空格） | 空格 |
| 48~57 | `0`~`9` | 数字 0 到 9 |
| 65~90 | `A`~`Z` | 大写字母 |
| 97~122 | `a`~`z` | 小写字母 |

> **关键值**：`'a' - 'A' = 32`，`'0' = 48`

---

## 附录：运算符优先级（从高到低）

| 优先级 | 运算符 | 结合性 | 说明 |
|--------|--------|--------|------|
| 1 | `()` `[]` `->` `.` | 左→右 | 函数调用、数组下标、成员访问 |
| 2 | `++` `--`（后缀） | 左→右 | 后缀自增/自减 |
| 3 | `++` `--` `+` `-` `!` `(类型)` `*` `&` `sizeof` | 右→左 | 前缀自增/减、正负号、逻辑非、类型转换、解引用、取地址、sizeof |
| 4 | `*` `/` `%` | 左→右 | 乘、除、取模 |
| 5 | `+` `-` | 左→右 | 加、减 |
| 6 | `<<` `>>` | 左→右 | 位左移、右移 |
| 7 | `<` `<=` `>` `>=` | 左→右 | 关系比较 |
| 8 | `==` `!=` | 左→右 | 相等/不等 |
| 9 | `&` | 左→右 | 按位与 |
| 10 | `^` | 左→右 | 按位异或 |
| 11 | `\|` | 左→右 | 按位或 |
| 12 | `&&` | 左→右 | 逻辑与 |
| 13 | `\|\|` | 左→右 | 逻辑或 |
| 14 | `?:` | 右→左 | 条件运算符 |
| 15 | `=` `+=` `-=` `*=` `/=` `%=` 等 | 右→左 | 赋值 |
| 16 | `,` | 左→右 | 逗号运算符 |

> **实用建议**：不要死记硬背优先级，有疑问时 **加括号** 即可。

---

## 附录：编译本项目

在 Linux 下编译：

```bash
gcc -o project1 -std=gnu99 $(find Project1 -name '*.c' | sort) -lm
```

- `-std=gnu99`：使用 GNU99 标准（支持 `//` 注释和 `gets`）
- `-lm`：链接数学库（提供 `pow`、`fabs`）

运行：

```bash
echo "hello world" | ./project1
```

> 本文档由 `Project1` 项目 46 份练习代码整理而成，旨在为 C 语言初学者提供一份全面的学习参考资料。
